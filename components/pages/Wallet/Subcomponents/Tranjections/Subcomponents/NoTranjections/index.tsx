const NoTranjections = () => {
  return (
    <div className="flex items-center flex-col min-h-[15.6rem] justify-center py-5 px-3">
      <h2 className="text-2xl font-medium mb-2">No transactions yet.</h2>
      <p className="text-lg font-medium text-gray-700">
        Want to try again with different dates?
      </p>
    </div>
  );
};

export default NoTranjections;
